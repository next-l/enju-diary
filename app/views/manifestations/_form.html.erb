<%= form_for(@manifestation) do |f| %>
  <% if @manifestation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manifestation.errors.count, "error") %> prohibited this manifestation from being saved:</h2>

      <ul>
      <% @manifestation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :original_title %><br />
    <%= f.text_area :original_title %>
  </div>
  <div class="field">
    <%= f.label :isbn %><br />
    <%= f.text_field :isbn %>
  </div>
  <div class="field">
    <%= f.label :date_of_publication %><br />
    <%= f.datetime_select :date_of_publication %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_area :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
